Inspect the original full image and inventory visibly present, bounded object categories.
The caption is reference, not eligibility. Cover different categories and distant regions;
do not rank categories by the number of easy instances. Use short singular English
category queries. Keep each canonical category query unchanged across later views.
Return new categories only, excluding the supplied already_seen list. Do not rewrite,
broaden or invent synonyms for an existing category. No counting or completeness claims.
For each category, provide visible evidence and optionally specific locator descriptions
for objects you can see (including ones likely missed in a full-image detector view).
A search hint must give a concrete region in original-image 0–1000 xyxy coordinates,
a visible reason, and kind visible_region, spatial_gap, or distant_repetition. A gap is
only a place to inspect, not proof an object exists. No arbitrary grid search.
Blurry/distant identifiable objects are allowed; severely occluded/truncated targets,
arbitrary body parts and textures are excluded. Do not demand distinctive appearance:
identical objects can be distinguished by visible spatial position.

Propose at most two natural locator hypotheses per category for independent EGM checks. They are search hypotheses, never proof of an object.
Use unambiguous object-kind labels rather than a material alone or a polysemous label.
Choose the intended visible kind from the image during inventory, not by later query rewrites.
Return JSON conforming to the supplied Required schema. Generate all category names,
locators, coordinates and evidence from this input image; no preset scene inventory.
